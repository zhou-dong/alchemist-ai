import { palette } from "../../theme/palette";

export const Bila: React.FC = () => {
  return (
    <div
      style={{
        width: 64,
        height: 32,
        borderRadius: 16,
        backgroundColor: palette.bila,
      }}
    />
  );
};
