import { palette } from "../../theme/palette";

export const Vera: React.FC = () => {
  return (
    <div
      style={{
        width: 72,
        height: 28,
        borderRadius: 14,
        backgroundColor: palette.vera,
      }}
    />
  );
};
