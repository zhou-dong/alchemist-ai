import { palette } from "../../theme/palette";

export const Radiatan: React.FC = () => {
  return (
    <div
      style={{
        width: 48,
        height: 48,
        borderRadius: "50%",
        backgroundColor: palette.radiatan,
      }}
    />
  );
};
