import { palette } from "../../theme/palette";

export const Predator: React.FC = () => {
  return (
    <div
      style={{
        width: 96,
        height: 48,
        borderRadius: 24,
        backgroundColor: palette.predator,
      }}
    />
  );
};
